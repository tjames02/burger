<h1>Burgers!</h1>

<h2>Burgers that I want to eat:</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>{{this.burger_name}}
      {{> burgers/burger-block devoured=false}}</li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that I've Eaten:</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>{{this.burger_name}}
      {{> burgers/burger-block devoured=true}}</li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
